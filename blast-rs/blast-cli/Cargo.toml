[package]
name = "blast-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "blast-cli"
path = "src/main.rs"

[dependencies]
blast-db = { path = "../blast-db" }
blast-core = { path = "../blast-core" }
clap = { version = "4", features = ["derive"] }
rayon = "1"
thiserror = "1"
